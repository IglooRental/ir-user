sudo: required

services:
- docker

language: java
dist: trusty

jdk:
- openjdk8

cache:
    directories:
    - $HOME/.m2

script:
- mvn clean package
- docker build -t ir-user:1.00 .

after_success:
- docker login -u '$DOCKER_USERNAME' -p '$DOCKER_PASSWORD'
- docker push jm5619/ir-user
